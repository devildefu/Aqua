#include "Modules/vga.h"
#include "Modules/keyboard.h"
#include "Modules/string.h"
#include "Modules/time.h"

void kmain(void) {
	clear();
	double d = 50.54;
	char str[6]]
	d2s(d,str,6,'.');
	puts(str);
	return;
}
